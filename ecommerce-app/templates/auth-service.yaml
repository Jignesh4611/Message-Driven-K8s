apiVersion: v1
kind : Service
metadata: 
  name: auth-src
  labels:
    app: auth

spec : 
  selector: 
    app: auth-src
  ports: 
   - name: http
     port : 80
     targetPort: 3000
  type: ClusterIP
