apiVersion: v1
kind : Service
metadata: 
  name: order-src
  labels:
    app: order

spec : 
  selector: 
    app: order-src
  ports: 
   - name: http
     port : 80
     targetPort: 3000
  type: ClusterIP
